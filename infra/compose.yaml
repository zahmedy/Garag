services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: garag
      POSTGRES_PASSWORD: garag
      POSTGRES_DB: garag
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: