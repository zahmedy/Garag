services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: garaj
      POSTGRES_PASSWORD: garaj
      POSTGRES_DB: garaj
    ports: ["5432:5432"]
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
